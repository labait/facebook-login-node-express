<% extend('layout') %>

<% if (!user) { %>
  <div>
  	<a href="/auth/facebook"><img src="login-fb.png" width="200"></a>
	</div>
<% } else { %>
	<h2>Hello, <%= user.displayName %></h2>
  <a href="/logout">Logout</a>
<% } %>
